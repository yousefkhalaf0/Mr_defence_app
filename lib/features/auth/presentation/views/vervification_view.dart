import 'package:flutter/material.dart';

class VervificationView extends StatelessWidget {
  const VervificationView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
